package config

/*
import redux.AppState
import redux.Epic

internal val configEpic: Epic <AppState> = { store, state, action, dep ->
    when (action) {
        is ConfigActions.Init -> {
            // TODO: call config dependency
            true
        }
        else -> false
    }
}
*/
